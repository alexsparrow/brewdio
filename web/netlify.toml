[build]
  # Since the Base Directory is already 'packages/marketing-site',
  # the command runs relative to this folder.
  command = "bun run build"
  publish = "dist"

[build.environment]
  # This only affects the Marketing Site build
  NODE_ENV = "production"
  BUN_FLAGS = "--ignore-scripts"
